# Two Sum

## 思路
最直接的方法是枚举所有数对，时间复杂度为 O(n²)。
为了降低复杂度，可以在遍历数组的同时使用哈希表记录已经访问过的元素及其下标。对于当前元素 `nums[i]`，只需查找 `target - nums[i]` 是否已经出现：
- 如果存在，则找到答案；
- 如果不存在，则将当前元素加入哈希表，继续遍历。

## 知识点
- Hash Table（哈希表）
- One-pass Hash Map（一次遍历）
- 空间换时间

## 复杂度
- 时间复杂度：**O(n)**
- 空间复杂度：**O(n)**
